# Known Issues

## v0.8.1 stable baseline
- No critical package integrity issues were found by `scripts/check-release.php`.
- This release does **not** add new business features.
- Real SMTP delivery and production database migration still require environment-specific testing.
- API submissions should still be manually tested after each deployment using the examples in `API-TEST-EXAMPLES.md`.
